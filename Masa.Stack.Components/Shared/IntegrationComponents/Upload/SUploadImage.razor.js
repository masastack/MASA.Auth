export function calculate() {
    // 获取容器和文本元素
    //const container = document.querySelector('.m-avatar');
    //const text = document.querySelector('.font-weight-medium');

    //// 获取文本的宽度
    //const textWidth = text.offsetWidth;

    //// 获取容器的宽度
    //const containerWidth = container.offsetWidth;

    //// 如果文本的宽度大于容器的宽度，则缩小文本
    //if (textWidth > containerWidth) {
    //    const fontSize = parseInt(window.getComputedStyle(text).fontSize);

    //    while (textWidth > containerWidth && fontSize > 12) {
    //        fontSize--;
    //        text.style.fontSize = `${fontSize}px`;
    //    }
    //}
}