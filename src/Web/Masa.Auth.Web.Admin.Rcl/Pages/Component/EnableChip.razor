@inherits AdminCompontentBase

<div class="@Class" style="@Style">
    <MChip Color="@Color" TextColor="@TextColor" Ripple="false">
        <span class="btn">@T(Value?"Enabled":"Disabled")</span>
    </MChip>
</div>