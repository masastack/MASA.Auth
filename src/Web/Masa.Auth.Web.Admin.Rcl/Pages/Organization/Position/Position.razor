@page "/organization/position"
@inherits AdminCompontentBase

<PageTitle>
    @T("Position")
</PageTitle>

<DefaultCard>
    <AutoHeight>
        <HeaderContent>
            <div class="d-flex mb-6">
                <SearchTextField @bind-Value="Search" />
                <MSpacer />
                <SButton Small Color="primary" OnClick="() => OpenAddApiResourceDialog()" BorderRadiusClass="rounded-lg">
                    <MIcon Size=18>@IconConstants.Add</MIcon>
                    <span class="ml-2 btn">@T("Add")</span>
                </SButton>
            </div>
        </HeaderContent>
        <AutoHeightContent>
            @{
                DataIndex = (Page-1)*PageSize+1;
            }
            <SDataTable
                FixedHeader
                Height="@("calc(100vh - 288px)")"
                Headers="GetHeaders()" 
                Items="Positions" 
                TItem="PositionDto" 
                ItemsPerPage="PageSize">
                <HeaderColContent Context="header">
                    <span>@header.Text</span>
                </HeaderColContent>
                <ItemColContent>
                    @{
                        if(DataIndex == Page*PageSize+1) DataIndex = (Page-1)*PageSize+1;
                    }
                    @switch (context.Header.Value)
                    {
                        case "Index":
                            <span>@(DataIndex++)</span>
                            break;
                        case nameof(PositionDto.Name):
                            <div class="hover-pointer" @onclick="()=> OpenUpdateApiResourceDialog(context.Item)">
                                <span>@context.Item.Name</span>
                            </div>
                            break;
                        case "Action":
                            <SIcon Tooltip="@T("Edit")" OnClick="()=> OpenUpdateApiResourceDialog(context.Item)">@IconConstants.Update</SIcon>
                            <SIcon Tooltip="@T("Delete")" Class="ml-6" OnClick="()=> OpenRemovePositionDialog(context.Item)">@IconConstants.Remove</SIcon>
                            break;
                        default:
                            <span>@context.Value</span>
                            break;
                    }
                </ItemColContent>
            </SDataTable>
        </AutoHeightContent>
        <FooterContent>
            <SPagination Class="mt-5" @bind-Page="Page" @bind-PageSize=PageSize Total=Total />
        </FooterContent>
    </AutoHeight>
</DefaultCard>

<AddPositionDialog @bind-Visible="AddPositionDialogVisible" OnSubmitSuccess=GetPositionsAsync />
<UpdatePositionDialog @bind-Visible="UpdatePositionDialogVisible" PositionId=CurrentPositionId OnSubmitSuccess=GetPositionsAsync />