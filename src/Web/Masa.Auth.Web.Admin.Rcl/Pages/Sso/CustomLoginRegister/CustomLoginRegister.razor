@page "/sso/customLogin"
@inherits AdminCompontentBase

<DefaultCard>
    <AutoHeight>
        <HeaderContent>
            <div class="d-flex mb-6">
                <SearchTextField @bind-Value="Search" />
                <MSpacer />
                <DefaultButton Small OnClick="() => OpenAddCustomLoginDialog()">
                    <MIcon Size=18>@IconConstants.Add</MIcon>
                    <span class="ml-2 btn">@T("Add")</span>
                </DefaultButton>
            </div>
        </HeaderContent>
        <AutoHeightContent>
            <DefaultDataTable Headers="Headers" Items="CustomLogins" TItem="CustomLoginDto" ItemsPerPage="PageSize">
                <HeaderColContent Context="header">
                    <span class="body2 regular3--text">@header.Text</span>
                </HeaderColContent>
                <ItemColContent>
                    @switch (context.Header.Value)
                    {
                        case nameof(ClientDto.LogoUri):
                            <MAvatar Size=48>
                                <img src="@context.Item.Client.LogoUri" />
                            </MAvatar>
                            break;
                        case nameof(CustomLoginDto.Name):
                            <span class="subtitle default--text">@context.Item.Name</span>
                            break;
                        case (nameof(ClientDto.ClientName)):
                            <span class="body regular--text">@context.Item.Client.ClientName</span>
                            break;
                        case (nameof(CustomLoginDto.CreationTime)):
                            <div class="body regular--text">@context.Item.CreationTime.ToString(T("$DateFormat"))</div>
                            <div class="body2 regular3--text">@context.Item.CreationTime.ToString("HH:mm:ss")</div>
                            break;
                        case nameof(CustomLoginDto.ModificationTime):
                            <div class="body regular--text">@context.Item.ModificationTime?.ToString(T("$DateFormat"))</div>
                            <div class="body2 regular3--text">@context.Item.ModificationTime?.ToString("HH:mm:ss")</div>
                            break;
                        case (nameof(CustomLoginDto.Modifier)):
                            <span class="body regular--text">@context.Item.Modifier</span>
                            break;
                        case nameof(CustomLoginDto.Enabled):
                            <EnableChip Value="@context.Item.Enabled" />
                            break;
                        case "Action":
                            <MIcon Small Color="regular" OnClick="()=> OpenUpdateCustomLoginDialog(context.Item)">@IconConstants.Update</MIcon>
                            <MIcon Small Color="regular" Class="ml-7" OnClick="()=> OpenRemoveCustomLoginDialog(context.Item)">@IconConstants.Remove</MIcon>
                            break;
                        default:
                            <span class="subtitle">@context.Value</span>
                            break;
                    }
                </ItemColContent>
            </DefaultDataTable>
        </AutoHeightContent>
        <FooterContent>
            <Pagination Class="mt-5" @bind-Page="Page" @bind-PageSize=PageSize Total=Total />
        </FooterContent>
    </AutoHeight>
</DefaultCard>

<AddCustomLoginRegisterDialog @key="Guid.NewGuid()" @bind-Visible="AddCustomLoginRegisterDialogVisible" OnSubmitSuccess=GetCustomLoginsAsync />
<UpdateCustomLoginRegisterDialog @key="Guid.NewGuid()" @bind-Visible="UpdateCustomLoginRegisterDialogVisible" CustomLoginId="CurrentCustomLoginId" OnSubmitSuccess=GetCustomLoginsAsync />