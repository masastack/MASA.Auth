@inherits AdminCompontentBase

<SheetDialog Value="Visible" ValueChanged=UpdateVisible Title="@T("Add User")">
    <MForm Model=IdentityResourceDetail EnableValidation @ref="Form">
        <div class="full-height flex-column">
            <div class="mt-6 flex-height-auto overflow-y">
                <div class="text-subtitle2">@T("Basic Information")</div>
                <MRow Class="mt-4">
                    <MCol>
                        <MTextField @bind-Value="IdentityResourceDetail.DisplayName" Label="@T("DisplayName")" HideDetails="@("auto")" Outlined />
                    </MCol>
                    <MCol>
                        <MTextField @bind-Value="IdentityResourceDetail.Name" Label="@T("IdentityResource.Name")" HideDetails="@("auto")" Outlined />
                    </MCol>
                </MRow>
                <MRow Class="mt-1">
                    <MCol>
                        <MTextField @bind-Value="IdentityResourceDetail.Description" Label="@T("Description")" HideDetails="@("auto")" Outlined />
                    </MCol>
                </MRow>
                <MRow Class="mt-1">
                    <MCol>
                         <MSwitch @bind-Value="IdentityResourceDetail.Required" Flat Label="@("Whether must:"+ (IdentityResourceDetail.Required ? T("True"):T("false")))"></MSwitch>
                    </MCol>
                </MRow>
                <div class="mt-6 text-subtitle2">@T("User Claims")</div>

            </div>
            <div class="d-flex mt-6">
                <MSwitch @bind-Value="IdentityResourceDetail.Enabled" Inset Class="label" Label="@(IdentityResourceDetail.Enabled ? T("Enable"):T("Disabled"))" />
                <MSpacer></MSpacer>
                <MButton OnClick="async () => await AddOrUpdatetIdentityResourceAsync(context)" Color="primary" Class="small-button ml-6" Text>@T("Submit")</MButton>
            </div>
        </div>
    </MForm>
</SheetDialog>