@page "/user"
@using Masa.Auth.Web.Admin.Rcl.Pages.Subjects.Users.Model
@inherits AdminCompontentBase

<DefaultCard>
    <ElevationTab Class="defaultTab full-height" TabMinWidth="120" Left Tabs="new List<string> { T(PageTab.User), T(PageTab.ThirdPartyUser), T(PageTab.Staff) }" Dense>
        <ElevationTabItem>
            <User />
        </ElevationTabItem>
        <ElevationTabItem>
            <ThirdPartyUser />
        </ElevationTabItem>
        <ElevationTabItem>
            <Staff />
        </ElevationTabItem>
    </ElevationTab>
</DefaultCard>

@code {
    StringNumber _pageTab = PageTab.User;
}